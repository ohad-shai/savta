<PreferenceScreen xmlns:app="http://schemas.android.com/apk/res-auto">

    <Preference
        app:key="@string/pref_key_full_name"
        app:title="@string/full_name" />

    <Preference
        app:key="@string/pref_key_email"
        app:title="@string/email" />

    <Preference
        app:key="@string/pref_key_change_password"
        app:title="@string/change_password" />

    <Preference
        app:key="@string/pref_key_delete_account"
        app:title="@string/delete_account" />

</PreferenceScreen>